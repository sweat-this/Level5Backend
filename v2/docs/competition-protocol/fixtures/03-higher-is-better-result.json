{
  "fixtureId": "higher-is-better-result",
  "protocolVersion": 1,
  "description": "A game whose primary comparison metric is HigherWins resolves in favor of the larger value.",
  "backendExecutable": true,
  "requiresDownstream": [],
  "seriesFormat": { "totalGames": 1 },
  "ruleset": {
    "rulesetId": "score-only",
    "rulesetVersion": 1,
    "informationPolicy": "SealedAttempt",
    "alternatesFirstAttempt": false,
    "comparisonKeys": [{ "metric": "Score", "direction": "HigherWins" }]
  },
  "commands": [
    { "op": "createChallenge", "by": "challenger" },
    { "op": "accept", "by": "opponent" },
    { "op": "startAttempt", "by": "challenger", "game": 1 },
    { "op": "startAttempt", "by": "opponent", "game": 1 },
    { "op": "completeAttempt", "by": "challenger", "game": 1, "result": { "Score": 90 } },
    { "op": "completeAttempt", "by": "opponent", "game": 1, "result": { "Score": 40 } }
  ],
  "expected": {
    "seriesStatus": "Completed",
    "winner": "challenger"
  },
  "notes": "The Backend's Score model is unconditionally higher-wins, so this fixture is trivially satisfiable today; it exists to give the same scenario a fixture id that fixture 04 (lower-wins) can be contrasted against."
}
